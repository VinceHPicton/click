## Go commands
- Prefer running the simple `go  test ./...` for your typical testing checks, only use more complex commands when necessary

## Regenerating sqlc code
- Always run `make` in the project root (/click) to regenerate sqlc code

